# Portfolio Build Instructions

You are building the portfolio for Anirban Bhowmik.

## Three design tools/layers

1. UI UX Pro Max = design intelligence. If the UI UX Pro Max skill is installed/available, use it before major visual redesigns to establish typography, palette, spacing, interaction and accessibility rules.
2. 21st.dev = component source/inspiration. Components should be copied/customized into `src/components/ui/`; do not create a dependency on 21st.dev unless explicitly required.
3. Motion = animation engine. Use `motion/react` for purposeful transitions, reveals, hover states, parallax and page relationships.

## Visual direction

Editorial + futuristic + AI. Use the provided reel references as inspiration: off-white dotted grid, oversized serif headlines, modern sans UI text, floating dark product/browser windows, premium whitespace, restrained shadows and cinematic motion. Do not copy another site.

## Truth policy

Only use verified information. Never invent awards, certifications, employers, project features, metrics, clients or links. KineticMesh is the current project name; do not list KIRO as a separate project.

## Build policy

Inspect the existing repository before changing it. Preserve useful work. After changes run `npm run build` and fix every error. Audit desktop, mobile, accessibility, performance, 3D rendering and console errors.
